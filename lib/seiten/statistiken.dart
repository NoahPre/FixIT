// statistiken.dart
// 1
import "package:flutter/material.dart";

class Statistiken extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Statistiken"),
      ),
      body: Center(
        child: Text("Noch keine Statistiken verfügbar"),
      ),
    );
  }
}
